class IsiData {
  String createdAt, id, baju, celana, harga, icon;

  IsiData({
    required this.createdAt,
    required this.id,
    required this.baju,
    required this.celana,
    required this.harga,
    required this.icon,
  });
}
